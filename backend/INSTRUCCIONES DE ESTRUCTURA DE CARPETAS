# ============================================
# INSTRUCCIONES DE ESTRUCTURA DE CARPETAS
# ============================================

# Crear estructura completa del proyecto:

mkdir -p backend/src/{config,controllers,middleware,models,routes,scripts,utils}
cd backend

# Crear archivos principales:
touch src/server.ts
touch src/config/database.ts

# Modelos
touch src/models/User.model.ts
touch src/models/Lab.model.ts
touch src/models/Reservation.model.ts

# Controladores
touch src/controllers/auth.controller.ts
touch src/controllers/lab.controller.ts
touch src/controllers/reservation.controller.ts

# Middleware
touch src/middleware/auth.middleware.ts
touch src/middleware/error.middleware.ts
touch src/middleware/validation.middleware.ts

# Rutas
touch src/routes/auth.routes.ts
touch src/routes/lab.routes.ts
touch src/routes/reservation.routes.ts

# Utilidades
touch src/utils/AppError.ts
touch src/utils/jwt.utils.ts

# Scripts
touch src/scripts/seed.ts

# Archivos de configuración
touch .env
touch .gitignore
touch tsconfig.json
touch nodemon.json
touch .prettierrc
touch .eslintrc.json
touch README.md